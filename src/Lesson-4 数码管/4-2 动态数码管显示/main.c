#include "defines.h"
#include "utilities.h"

void main()
{
    while (TRUE)
    {
        NixieTubeNumberDisplay(0, 5);
        NixieTubeAlphaDisplay(1, 'b');
        NixieTubeAlphaDisplay(2, 'g');
        NixieTubeAlphaDisplay(3, 'a');
    }
}